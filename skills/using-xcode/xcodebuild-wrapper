#!/usr/bin/env fish

# Run xcodebuild through xcsift, https://github.com/ldomaradzki/xcsift
xcodebuild $argv 2>&1 | xcsift -f toon --warnings --toon-key-folding safe
set -l exit_code $status

exit $exit_code
